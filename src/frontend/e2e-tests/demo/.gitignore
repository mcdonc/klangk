# Recording / re-take artifacts (never committed). These dirs hold the
# Xvfb/ffmpeg captures (.mp4), Playwright's downscaled .webm fallbacks, and
# screenshot .png diagnostics.
recordings/
demo-videos/
test-results/
*.mp4
*.webm
*.png

# node_modules is a symlink to ../node_modules; never commit it
node_modules

# Python (cli_demo.py)
__pycache__/
*.pyc
