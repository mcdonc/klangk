# Default registry search list (Debian's podman ships this unset). Lets the
# bare `klangk-base` / `klangk` short names and seeding resolve docker.io. At
# runtime the backend creates workspace containers with `--pull=never`, so no
# registry is contacted once the workspace image is in the store.
unqualified-search-registries = ["docker.io"]
