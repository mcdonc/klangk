{
  "name": "@klangk/chat",
  "version": "1.0.0",
  "description": "Workspace chat feature — chat tab and chat agent UI",
  "klangk": {
    "config": {
      "KLANGKWS_FEATURE_CHAT_AGENT_ENABLED": {
        "description": "Enable the chat agent in chat. Opt-in (default off / empty).",
        "default": "",
        "scope": "both"
      }
    }
  }
}
