name: klangk_feature_chat
description: Workspace chat feature (chat tab + clanker agent UI) for Klangk
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter
  klangk_plugin_api:
    git:
      url: https://github.com/mcdonc/klangk-plugin-api.git
      ref: v0.5.1
  provider: ^6.1.0
  flutter_markdown_plus: ^1.0.7
  markdown: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
