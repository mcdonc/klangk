{
  "name": "@klangk/boingball",
  "version": "1.0.0",
  "description": "Boing Ball demo — bouncing checkered ball overlay with wrapped text",
  "pi": {
    "extensions": [
      "./extension.ts"
    ]
  },
  "klangk": {
    "config": {
      "KLANGK_BOING_TEXT": {
        "description": "Text to wrap around the bouncing ball",
        "default": "Klangk",
        "scope": "frontend"
      },
      "KLANGK_BOING_SPEED": {
        "description": "Animation speed multiplier (1.0 = normal, 2.0 = double speed)",
        "default": "1.0",
        "scope": "frontend"
      }
    }
  }
}
